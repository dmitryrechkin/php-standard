--- a/SlevomatCodingStandard/Helpers/ReferencedNameHelper.php
+++ b/SlevomatCodingStandard/Helpers/ReferencedNameHelper.php
@@ -450,9 +450,12 @@ class ReferencedNameHelper
 		$referencedName = self::getReferenceName($phpcsFile, $pointer, $referencedNameEndPointer);
 
+		// Fix for attribute_closer being null - use attributeStartPointer as fallback
+		$endPointer = $tokens[$attributeStartPointer]['attribute_closer'] ?? $attributeStartPointer;
+
 		$referencedNames[] = new ReferencedName(
 			$referencedName,
 			$attributeStartPointer,
-			$tokens[$attributeStartPointer]['attribute_closer'],
+			$endPointer,
 			$referenceType,
 		);